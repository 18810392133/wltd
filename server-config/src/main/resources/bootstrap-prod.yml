server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://app.iotspace.net:8761/eureka/

spring:
  application:
    name: server-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/